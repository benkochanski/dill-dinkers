// SPA mode: rendered entirely on the client (no SSR). All data fetching
// happens in the browser against the Apps Script JSON endpoint.
export const ssr = false;
export const prerender = false;
export const trailingSlash = 'never';
