# Apps Script /exec URL — the public JSON endpoint lives at
#   $PUBLIC_API_BASE?page=json&action=...
# Get it from: Apps Script editor → Deploy → Manage deployments → /exec URL
# Use the production /exec deployment, NOT /dev (which only the script owner can hit).
PUBLIC_API_BASE=https://script.google.com/macros/s/REPLACE_ME/exec
